# 내 작업 규칙

> 이 파일은 **짧을수록 좋습니다.** 30줄을 넘기지 마세요.
> 채우는 법: Claude Code에서 `CLAUDE.md 열어서 나에 대해 물어보고 채워줘`

## 나는

- 이름 ·
- 하는 일 ·
- 주로 다루는 것 ·

## 결과물은

- 말투 · `docs/내문체.md` 를 따릅니다 (없으면 `내 문체 기준서 만들어줘`)
- 만든 파일은 `결과물/` 에 둡니다
- 작업이 끝나면 `기록/worklog/오늘.md` 에 한 줄 남깁니다

## 일하는 방식

- 큰 파일은 **통째로 읽지 않습니다.** 구조부터 보고 필요한 부분만 엽니다
- 긴 결과는 화면에 뿌리지 말고 **파일로 만들고 경로만** 알려줍니다
- 숫자와 사실에는 출처를 붙입니다. 없으면 `[출처 필요]` 로 비워 둡니다

## 하지 않을 것

- 고객 개인정보(이름+연락처)를 파일이나 대화에 남기지 않습니다
- `.env` 의 값을 화면에 출력하지 않습니다
- 원본 파일을 덮어쓰지 않습니다. 사본을 만듭니다
- 모르는 것을 그럴듯하게 메우지 않습니다. 비워 두고 무엇이 필요한지 적습니다
